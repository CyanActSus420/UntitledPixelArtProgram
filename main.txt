; put your code here
; you can also use the --file launch argument to use a custom file (--file=[file name])

; sets byte 0 to 1
SET 1 0

; prints byte 0
PRINT 0

; sets byte 2 to 5
SET 5 2

; draws a pixel at coordinates x = byte 2 and y = byte 2, with color set to whatever byte 2 is (in our case, 5)
DRAW 2 2 2
